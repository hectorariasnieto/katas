import { fruitCounter } from './fruit-counter'

describe('fruitCounter', () => {
  it(
    'Sould return an object whose keys are the unique elements,' +
      'and the value is the count of each repeated element',
    () => {
      const given = []

      const actual = fruitCounter(given)

      expect(actual).toBe()
    }
  )
})
